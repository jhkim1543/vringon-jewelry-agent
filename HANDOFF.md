# VRINGON Jewelry Agent 인수인계

새 대화창에 이 문서를 그대로 붙여넣고 "이어서 진행해줘"라고 말하면 됩니다.

---

## 0. 먼저 읽을 것

작업 폴더는 `C:\Users\rebui\Downloads\local_handoff\local_handoff\vringon-jewelry` 입니다.
주얼리 전용 제품이고, 신발(`../vringon-shoe`)과 분리된 별도 저장소입니다.

| 항목 | 값 |
| --- | --- |
| 저장소 | https://github.com/jhkim1543/vringon-jewelry-agent (public) |
| 데모 | https://jhkim1543.github.io/vringon-jewelry-agent/ |
| 스택 | Vite + React 18 + TypeScript, @xyflow/react 12 |
| 개발 서버 | `npm run dev` (Vite 미들웨어가 API 프록시) · 포트 5191 은 워크스페이스 launch.json 의 `vringon-jewelry-alt` |
| 배포 | `npm run build:pages` 로 `docs/` 생성 → 커밋 → GitHub Actions |

### 절대 어기면 안 되는 보안 규칙

- API 키는 `vringon-jewelry/.env` 에만. **`VITE_` 접두사 금지** (번들에 들어간다).
- 키를 읽는 곳은 `server/*.mjs` 뿐이다.
- 푸시 전 staged 시크릿 스캔 0 확인. 키를 커밋하지 않는다.
- Bright Data 토큰 권한을 Admin 으로 올리지 않는다.

### 실행 함정 (전부 실제로 겪은 것)

- **개발 서버를 브라우저 패널로 띄우면 패널이 숨겨질 때 죽는다.** 헤드리스 샘플 생성은
  `cmd /c "npx vite --port 5191 --strictPort"` 를 별도 프로세스로 띄우고 돌릴 것.
  죽은 서버로 돌리면 조사가 전부 실패 폴백이 된다 — make-sample 이 시작 전에 `/api/status` 로 확인한다.
- **실행 중 `server/*.mjs` 를 수정하지 말 것.** Vite 가 재시작하며 진행 중인 조사 호출이 전부 끊긴다.
- Node fetch 는 헤더 대기 300초에서 "fetch failed" 로 끊는다. 서버·스크립트 모두 undici Agent 로 늘려 두었다.
- heredoc 으로 정규식/백슬래시를 쓰면 `\` 가 조용히 사라진다 — 스크립트 수정은 Edit 도구로.

---

## 1. 현재 아키텍처 · 3-에이전트 (2026-08-22 전면 개편 · 2026-08-26 대개편)

### 2026-08-26 대개편 요약

- **다중 지역**: `RunParams.countries: string[]` (옛 저장본의 `country` 는 `regionsOf()` 가 감싼다).
  편집샵·런웨이·확산 조사가 지역별로 돌고, 지역 수만큼 결과와 시간이 늘어난다.
- **프롬프트도 분석 언어로**: direction·title·final_prompt 모두 langName. 캐시 키
  agp1에서 agp3, agip1에서 agip2 로 올렸다(langName 이 키에 들어감). 수집량도 늘렸다(agc2·ags2).
- **방향 강제 취급 규칙**(agr2): 방향에 명시된 속성은 주류가 아니어도 반드시 다룬다 —
  Gemini 감리(`npm run direction:review`)가 "무광 실버 지시가 리포트에서 사라진 것"을 잡아 넣은 규칙.
  감리는 조사를 만든 모델(OpenAI)이 아닌 Gemini 로 한다. 같은 모델 자기 심사는 후하다.
- **새로 만들기**: 에이전트 픽커(다크 히어로 · GPT 생성 이미지 3장, `scripts/make-brand-art.mjs`)
  + 번호 스테퍼 아코디언 + 우측 "실행 전 검토"(수량·예상 시간, 가격 없음 · `estimateMinutes`).
- **덱 템플릿 2종**: 트렌드 = 종이색 에디토리얼(tpl-trend), 경쟁사·편집샵·런웨이 = 아이보리
  럭셔리(tpl-lux). 사진은 전부 contain — cover 는 세로 사진을 잘라 먹는다. 사진 없으면
  "이미지 확인 필요" 칸으로 정직하게.
- **보드 = Miro 식 협업**: `server/board-api.mjs` (SSE) + `src/core/boardLive.ts`.
  링크 방문자도 편집(메모·텍스트·이미지·핀 댓글·카드 이동), 실시간 커서(이름표),
  게스트 이름 자동 부여·수정 가능. 결과 카드는 문서(snodes)에 스냅숏으로 밀어 넣어
  분석이 없는 브라우저에서도 열린다. 발표 모드는 위에서 아래 순서로 확대, 핀은 제외.
  정적 배포에서는 로컬 전용으로 동작하고 그렇게 말해 준다.
- **테마 대개편**: 라이트 = "인쇄된 룩북"(아이보리 종이·웜 섀도·골드 괘선·세리프 디스플레이),
  다크 = "밤의 쇼룸"(차콜 네이비·인디고 글로우·링 섀도). GPT 로 두 콘셉트 목업을 생성해
  분석한 결과를 tokens.css 의미 토큰과 theme.css 질감 규칙으로 옮겼다. 반전이 아니라 다른 재질.
  골드(#A08A55)는 3.2:1 이라 장식·18pt+ 전용, 본문 금지.

옛 스펙·룰·QA·MD·브랜드 파이프라인은 **전부 제거**됐다. 디자인은 이제
"조사 → 레퍼런스 → DNA → 프롬프트 쌍 → 생성"의 계보로 나온다.

### 에이전트 3종 (`Mode`)

| id | 이름 | 흐름 |
| --- | --- | --- |
| `competitor` | 경쟁사 트렌드 | 경쟁사 크롤(대표/베스트/6개월 신상) + 국가별 편집샵 10곳 → 방향 기반 트렌드 리포트 → 슬롯 10 레퍼런스 → DNA→P/T/R/C/A→최종 프롬프트 → 생성 |
| `fashion` | 패션 트렌드 | 런웨이·컬렉션 + 리테일 확산 신호 → 패션→주얼리 번역 리포트 → 패션 룩 레퍼런스 10 → Fashion DNA(+Complement) → 생성 |
| `collection` | 주얼리 컬렉션 | 키워드 문화 조사 → 10축 추상화 → 세트 콘셉트(1/3/5) + Form·Motion·Material·Atmosphere 콘셉트 아트 + 라인업 → 세트 DNA 고정, 품목별 프롬프트 → 생성 |

### 유저 입력 (스펙 그대로, 그 이상 받지 않는다)

공통: 검색 지역 · 분석 언어(ko/ja/en/zh/fr/it) · 조사 방향(자유 텍스트) · 타겟 연령/성별.

검색 지역은 여섯 권역(한국·일본·유럽·아시아·중동·미국)이고 자유 입력도 받는다.
**담기는 값은 언제나 영문 표준명**(`Korea`, `Japan`, …)이고 화면에서만 번역한다 — `REGIONS`,
`regionLabel()`, `regionValue()` 가 `types.ts` 에 있다. 번역된 글자를 담으면 화면 언어를 바꾼 순간
칩 선택이 풀리고, 같은 지역이 언어마다 다른 조사 캐시를 만든다(실제로 그랬다).
지역을 화면에 찍는 곳은 전부 `regionLabel()` 을 거친다.
경쟁사: + 경쟁사 목록, 품목 1, 수량 10/20/30/40. 패션: 품목 1, 수량. 컬렉션: 키워드가 방향 자리,
품목 다중(링/이어링/네크리스/펜던트/브레이슬릿), 세트 1/3/5, 고급 설정(표현 수준·포지셔닝·금속/스톤 선호·제외).

수량 → 변형: 10=기본, 20=+상업, 30=+형태 실험, 40=+소재·구조 실험 (`VARIANTS_FOR`).
상업 변형 규칙: 색만 바꾸면 안 되고 크기·구조·착용·공정 중 둘 이상이 달라야 한다 — 프롬프트에 박혀 있다.

### 파일 지도

- `server/agents-api.mjs` — 조사·프롬프트 전부. `ask()`(웹 검색+스키마) 재사용, 레퍼런스 이미지 판독만
  `askVision`(gpt-5 + input_image). 캐시 키: `agc2`(경쟁사) `ags2`(편집샵) `agr2`(리포트) `agrw1`(런웨이)
  `agad1`(확산) `agsel1`(레퍼런스) `agdna2`(DNA) `agp3`(프롬프트) `agk1`(키워드) `agset1`(세트) `agip2`(품목) `agsc1`(평가).
  **프롬프트를 바꾸면 해당 키 버전을 올릴 것** — 안 올리면 옛 캐시가 나온다.
- `server/tidy.mjs` — 사람이 읽는 문장 다듬기. 조사 모델은 놔두면 대시를 문장 부호로, 화살표를 순서
  표시로, 줄 앞에 `- ` 불릿을 쓴다. `LANG_RULE` 이 프롬프트로 금지하고, 이 파일이 뒤에서 한 번 더 거른다
  (규칙만으로는 종종 어긴다). `agents-api.mjs` 의 `cached()`/`save()` **내보내는 쪽**에 걸려 있어서,
  다듬는 규칙을 고쳐도 조사를 다시 돌릴 필요가 없다. 캐시에는 모델이 준 원문이 남는다.
  건드리지 않는 것: 주소(자리표로 빼 두고 되돌린다), `prompt`/`final_prompt`(한 글자만 달라져도
  이미지 캐시가 빗나가 전부 재과금), 숫자 앞 `+`(뜻이 바뀐다), 붙여 쓴 하이픈(`18-24`는 범위다).
- `server/grab.mjs` — 제품 사진 받아 오기 **한 곳**. 직링크 → 상품 페이지 og:image → 유료 언블로커 순으로
  시도하고 `.cache/shots` 에 성공·실패를 모두 기억한다. `/api/shot` 과 레퍼런스 DNA 가 같이 쓴다.
  전에는 두 벌이었는데, DNA 쪽이 `unlockedFetch`(`{buf,type}` 반환)를 `Response` 로 알고 `r.ok` 를 봐서
  **언제나 null** 이었다. 사진이 한 장도 비전 모델에 안 들어갔고, 화면에는 "이미지 미확인" 이라고
  정직하게 찍혀서 사이트가 막은 것처럼 보였다. 그 결과 디자인 열 개가 설명글만 보고 나왔다.
  S4 끝에 `사진 N/10` 총계를 남기는 이유가 이것이다 — 0/10 이면 고장으로 보인다.
- `src/core/types.ts` — 전 도메인 타입. `RunState.algo === 2` 가 신세대 표식. 옛 저장본은 목록에서 걸러진다.
- `src/core/pipeline.ts` — 세 흐름. 게이트 없음. `regeneratePair`(개별 재생성·버전 추가),
  `scoreFinishedPairs`(텍스트 기준 일괄 평가 — 비전 아님, 화면에도 그렇게 표기).
- `src/core/agents.ts` — 클라이언트 fetch 계층. `analysisLang → langName` 명시 전달.
- `src/core/agentDeck.ts` — 16:9 덱(경쟁사/편집샵/트렌드/런웨이/확산/키워드). `deck.ts` 의 slide/print/download 재사용.
  경쟁사 표지는 **로고 파일 대신 브랜드명 타이포** — 남의 로고를 무단 게재하지 않는다.
- `src/ui/RunView.tsx` — 분석 탭. 허브(실데이터 막대그래프) → 덱 → 세트/레퍼런스 → 쌍(수정·재생성·버전).
- `src/core/boardModel.ts` + `src/ui/Board.tsx` — 협업 보드. 결과 카드(레퍼런스 2장 + 쌍 스포트라이트)와
  사람이 붙인 것(메모·텍스트·이미지·핀)이 한 캔버스. 실시간은 `boardLive.ts` + `server/board-api.mjs`.
  발표 모드 = 캔버스 위→아래 순서로 전체 화면(←/→, Esc, 썸네일), 핀 제외.
- `src/App.tsx` — 실행 후·내역에서 열 때 모두 **분석 탭 먼저**.

### 정직성 계약 (agent-audit 가 검사)

- 편집샵/확산 순위는 `official_best` 와 `exposure` 를 구분한다. **노출을 판매라 부르지 않는다.**
- 조사 실패는 실패로 적고 멈춘다. 샘플 데이터 폴백은 없다.
- 레퍼런스 10개는 슬롯별 다른 트렌드 조합으로 뽑고, 각각 이유·출처·조합을 단다.
- 이미지를 못 내려받은 레퍼런스의 DNA 는 "서술 기반"으로 표기된다(`hadImage`).
  사진은 보탬이지 조건이 아니다 — 비전 호출이 사진 때문에 400 을 내면 사진 없이 한 번 더 간다.
  안 그러면 못 읽는 사진 한 장이 디자인 하나를 통째로 없앤다(실제로 그랬다: svg 자리표를 보내
  "valid image 가 아니다" 로 400). 비전에 보낼 형식은 png/jpeg/webp/gif 로만 좁혀 두었다.
- 평가 점수는 프롬프트 텍스트 기준 사전 점검이다 — 비전 평가처럼 말하지 않는다.

### 이미지 생성

기존 엔진 경로 그대로 (`/api/image/generate`). `.env` 에 `SELF_HOST_IMAGE_URL/MODEL/KEY` 를 넣으면
자체 호스팅(SenseNova 등 OpenAI 호환 서빙)으로 전환되고 폴백하지 않는다. `npm run selfhost:probe` 로 검증.
이 PC(Intel Arc, CUDA 없음)에서는 SenseNova 를 못 돌린다 — GPU 확보 후 프로브부터.

---

## 2. 검사와 샘플

```bash
npm run audit          # i18n 누락 + 문구 다듬기 + 에이전트 샘플 계약, 셋 다
npm run sample:make -- .sampleruns/sample_competitor_ring.cfg.json --go   # 서버 5191 필요
npm run sample:diff    # 구운 샘플끼리 얼마나 다른 결과인지 수치로 본다
```

- 문구 감사(`scripts/copy-audit.mjs`): 화면에 나가는 문장에서 em/en 대시·화살표·숫자 앞 `+`·
  한국어에 섞인 영어·종결 불일치를 잡는다. **살아 있는 키만** 본다(죽은 옛 항목까지 세면 고칠 게
  아닌 것으로 실패가 난다). 사전뿐 아니라 **구운 샘플의 생성 본문까지** 본다 — 화면 글의 대부분은
  조사 모델이 쓴 것이라, 사전만 보다가 106건을 놓친 적이 있다.
- 샘플 차이(`scripts/sample-diff.mjs`): 트렌드 축·라벨·레퍼런스 제목·프롬프트 낱말의 자카드 겹침을 잰다.
  같은 에이전트를 다른 입력으로 돌렸는데 프롬프트가 55% 넘게 겹치면 수렴 의심으로 실패시킨다.
  "비슷해 보인다" 는 느낌으로는 수렴을 못 잡는다.
  **트렌드 축이 90% 나오는 것은 수렴이 아니다** — 축은 `ELEMENT_AXES` 로 고정된 분류라 어느 실행이든
  같은 이름이 나온다. 갈리는지 봐야 할 곳은 그 아래 라벨과 프롬프트고, 판정도 프롬프트만 본다.

### 구워 둔 샘플 6종 (에이전트당 2건)

| 이름 | 에이전트 | 지역 | 품목 | 방향 |
| --- | --- | --- | --- | --- |
| `sample_competitor_ring` | 경쟁사 | 한국 | 반지 | 조각적인 실버·볼드 (Pandora·Mejuri) |
| `sample_competitor_earrings` | 경쟁사 | 유럽 | 귀걸이 | 하이주얼리 컬러스톤을 데일리로 (Tiffany·Monica Vinader) |
| `sample_fashion_necklace` | 패션 | 유럽 | 목걸이 | 런웨이 레이어드를 목걸이로 |
| `sample_fashion_earrings` | 패션 | 일본 | 귀걸이 | 아카이브 미니멀·무광 실버 |
| `sample_collection_horse` | 컬렉션 | 한국 | 반지·귀걸이·목걸이 | 2026 말의 해, 갈기의 결 |
| `sample_collection_tide` | 컬렉션 | 한국 | 반지·펜던트 | 제주 조수, 파식 지형 |

일부러 지역·품목·방향·타겟을 벌려 두었다. 그래도 결과가 닮으면 입력이 닮아서가 아니라
파이프라인이 수렴하는 것이므로 `sample:diff` 가 잡는다.

실측(2026-08-26 기준): 같은 에이전트를 다른 입력으로 돌린 두 쌍의 프롬프트 겹침은
컬렉션 23%, 패션 32% 로 문턱 55% 아래다. 트렌드 **라벨**과 레퍼런스 제목은 어느 짝에서도 겹침 0%.
축 이름만 90~100% 인데 그건 고정 분류라 그렇다.

- i18n: 영문 원문이 곧 사전 키. `t()`/`tf()` + `Record 맵` + `label:` 필드까지 스캔. 누락은 조용히 영어로 남는다.
  새 키는 `--dump` 로 틀을 뽑아 번역 후 `npm run i18n:merge -- <조각.json>`.
- 샘플: `.sampleruns/*.cfg.json` 이 설정. make-sample 이 원격 사진을 `/api/shot` 으로 굳히고(`shot_*.jpg`),
  서버 `/api/dev/save-sample` 이 생성 이미지를 복사하며 URL 을 `/samples/` 로 바꾼다.

### 레퍼런스 선정의 id 정합 (실제로 두 번 깨졌다)

- 조사 모델이 편집샵 name 칸에 "상호 · URL · 설명"을 통째로 넣을 때가 있다. 그대로 후보 id 를
  만들면(`sh-Korea-아몬즈 · https://…-0`) 선정 모델이 **깔끔하게 고쳐 돌려준 id** 와 어긋나
  레퍼런스 8/10 이 제목·사진·출처를 잃는다. pipeline 의 `cleanName()` 이 '·' 앞까지만 쓴다.
- 한글 id 는 NFC/NFD 어긋남도 방어해 둔다(`nfcIndex`).
- 프롬프트 캐시 키에는 DNA 가 들어간다(agp4). 전에는 refId 만 보고 캐시해서, 사진을 못 본
  고장 DNA 로 만든 프롬프트가 DNA 를 고친 뒤에도 재사용됐다.
- 이 계열 고장은 `agent-audit` 의 "출처 없는 레퍼런스 / 사진 N/10" 검사가 잡는다.

### 협업 보드 주의점

- 서버 문서는 `.cache/boards/<runId>.json`. 노드 단위 마지막 쓰기 승리 — 품평 규모에는 충분.
- 커서는 저장하지 않고 SSE 로 흘려보낸다. 6초 조용하면 화면에서 걷는다.
- 보드 이미지 업로드는 6MB 제한, `.cache/boards/assets` 에 굳는다.
- Windows curl 로 한글 op 를 보내면 CP949 로 깨진다 — 브라우저(fetch)는 UTF-8 이라 정상.

### 깊은 조사 (gpt-5-pro)

- `o3-deep-research` 계열 4종은 **2026-07-23 서비스 종료**. 종료 모델은 목록·단건 조회에 남아
  200 을 주면서 호출만 404 라 "권한 없음"으로 오진하기 쉽다 — 판별은 shutdown_date.
  `/api/research/deep-check` 가 이제 그렇게 판별해 안내한다.
- 대체는 **gpt-5-pro** (web_search + strict json_schema 실측 통과 · 단건 4분 안팎).
  `.env` 의 `OPENAI_DEEP_RESEARCH=1` 로 켜고, 서버가 트렌드 리포트 단계에만 전용 키로 태운다.
- 깊은 모드에서는 하위 질문 +2, 질문당 검색 10회, 축당 트렌드 3~5개·근거 2~3문장으로 상세화.
  얕은 경로 프롬프트는 바이트 동일하게 남겨 캐시를 보존했다.
- **시간은 범위로 보여 준다**: 새 분석 패널(전체 + 조사/생성 분해 + "깊은 조사 켜짐" 배지),
  실행 화면의 시간 밴드(경과·예상 범위 게이지·초과 안내), 단계 칩(예상 범위 → 완료 시 실측 분).
  계산은 `estimateStages()` 한 곳 — 화면끼리 어긋나면 안 된다.

### 깊은 조사 운영 주의 (실제로 겪은 것)

- **켜져 있는 동안 트렌드 리포트 호출은 전부 비싸다.** 시험 삼아 이 라우트를 두드리지 말 것 —
  스모크 한 번이 곧 gpt-5-pro 수십 분이다. 시험은 얕은 스위치(OPENAI_DEEP_RESEARCH=0)에서.
- **클라이언트가 끊겨도 서버는 계산을 계속한다.** 그 사이 같은 요청이 오면 캐시 미스로 같은
  조사가 처음부터 또 돌았다(세 겹까지 겹쳤다). `inflightReports` 합류가 이제 막는다.
- 벽시계 실측은 PC 슬립에 오염되기 쉽다. depth 2 깊은 리포트의 결과 물증: 검색 72회(얕은
  조사의 약 3.5배), 축 10개, 라벨 38개(축당 3.8), 방향 속성(무광) 21회 언급.

## 3. 남은 작업 (우선순위)

1. **조사 호출의 보이지 않는 재시도** — `research-api.mjs` 의 `ask()` 는 headersTimeout 20분에서
   끊고 최대 3번까지 조용히 다시 건다. 무거운 크롤이 20분을 넘으면 성공할 수 있던 호출이 잘리고,
   화면에는 한 시간 가까이 아무 로그도 안 남는다. 재시도를 로그로 드러내거나
   `background: true` + 폴링(딥리서치 경로가 이미 쓰는 방식)으로 바꿀 것.
2. 덱 PPT 내보내기(PptxGenJS)는 미구현 — 현재는 16:9 HTML 덱 + 인쇄(PDF)·HTML 다운로드.
3. 발표 모드 자동 재생·발표자 메모 미구현.
4. 컬렉션 세트별 재생성 시 "같은 세트의 다른 제품 참조" 는 프롬프트에 세트 DNA 로만 반영 — 이미지 상호 참조는 안 한다.
5. 평가 점수 기준 미달 자동 재생성 미구현 (수동 재생성은 됨).
6. 사전의 옛 키 800여 개가 죽어 있다 — 해는 없지만, 정리하려면 안전한 프룬을 새로 짜야 한다
   (자동 프룬을 한 번 시도했다가 두 줄짜리 항목에서 사전을 깨뜨려 되돌렸다).

## 4. 새 대화에서 첫 명령으로 쓸 문장

> 위 인수인계 문서대로 이어서 진행해줘. 먼저 `npm run audit` 와 `npm run sample:diff` 를 돌려
> 통과를 확인하고, 남은 작업 1번(조사 호출의 보이지 않는 재시도)부터 진행해줘.
> 보안 규칙과 실행 함정은 반드시 지켜줘.
