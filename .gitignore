node_modules
dist
# docs 는 GitHub Pages 배포 산출물이라 커밋한다
.env
.env.local
*.local
.cache
# 샘플 생성 설정은 커밋한다(.sampleruns/*.cfg.json). 실행 중간 산출물·로그는 뺀다
.sampleruns/*.log
.vite-detached.*
.*-probe.*
*.state.json
deploy/eb-stage
deploy/eb-bundle.zip
